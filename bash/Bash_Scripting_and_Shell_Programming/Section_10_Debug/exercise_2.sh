#!/bin/bash -x

# Exercise 2:
#
# Modify the previous exercise so that script continues, 
# even if an error occurs. This time all three ls commands will execute.                                                         

ls /etc/passwd
ls /move/along/nothing/to/see/here
ls /etc/passwd
